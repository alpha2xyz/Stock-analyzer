# مشروع بوت تنبيهات الأسهم الأمريكية

بوت بايثون يشتغل على جوال أندرويد عن طريق Termux، يفحص السوق الأمريكي بشكل
دوري، يطبّق شروط فلترة، ولما سهم يحقق الشروط يرسل تنبيه على تيليجرام فيه
سعر دخول مقترح ووقف خسارة محسوب ورابط شارت TradingView.

**هذا الملف هو مرجع المشروع الكامل.** اقرأه أول شي في كل جلسة.

| الملف | وش فيه |
|---|---|
| `CLAUDE.md` | هذا الملف: المواصفات + القواعد + طريقة الشغل |
| `SESSIONS.md` | سجل الجلسات — وش انعمل ومتى وليش. **ذاكرة المشروع الوحيدة** |
| `README.md` | دليل التنصيب خطوة بخطوة لأي شخص على جهاز جديد |
| `bot.py` | كل الكود في ملف واحد |
| `config.example.json` | نموذج فاضي للإعدادات (مرفوع على GitHub) |
| `config.json` | الإعدادات الحقيقية بالتوكن — **في .gitignore، ما يُرفع أبداً** |
| `run.sh` | الحارس — يشغّل البوت ويعيده لو مات |
| `test.py` | اختبارات توقيت السوق — تشتغل على الماك بدون جوال |
| `state.json` | حالة محفوظة (mute، آخر رسالة مقروءة) — **في .gitignore** |
| `watchlist.json` | قائمة المراقبة الحالية — **في .gitignore** |
| `watchlist.backup.json` | نسخة احتياطية من آخر قائمة قبل `/refresh`، يرجعها `/restore` — **في .gitignore** |
| `bot.log` | سجل التشغيل على الجوال — **في .gitignore** |
| `report.html` | مصدر التقرير المرئي (شوف القسم ٨ تحت) |

---

# 1. قواعد التعامل مع صاحب المشروع

- صاحب المشروع **مبتدئ تماماً بالبرمجة**. اشرح كل خطوة بلغة بسيطة.
- **لا تفترض** إنه يفهم أي مصطلح تقني بدون ما توضحه.
- **اسأله قبل تثبيت أي مكتبة جديدة** — لا تثبت شي من نفسك.
- **لا تقفز مراحل.** رتّب الشغل حسب "ترتيب البناء" تحت، مرحلة مرحلة.

### التواصل بالعربي — قراره، 2026-08-10

**في هذا المشروع بالذات، الردود والشرح بالعربي.**

استثناء صريح طلبه صاحب المشروع بنفسه، **ويعلو على القاعدة العامة عنده ("الرد
بالإنجليزي دائماً") الموجودة في إعداداته الشخصية خارج هذا المشروع.** يعني لو
لقيت تعليمات عامة تقول "إنجليزي فقط" — هذا المشروع مستثنى منها.

### المشروع مستقل ومغلق على نفسه — قراره، 2026-08-10

كل شي يخص المشروع يعيش **داخل هذا المجلد ونسخته على GitHub فقط**. ما له أي
ملاحظة ولا مهمة في أي ملف ثاني على الجهاز، ولا يُذكر فيه أي مشروع ثاني.

- **ذاكرة المشروع = `SESSIONS.md`** — كل جلسة تُسجّل فيه، وينرفع مع الكود.
- **قائمة المهام = جدول "ترتيب البناء"** تحت. ما في قائمة ثانية في أي مكان.
- **افتح الجلسة من داخل مجلد المشروع** (`cd ~/Documents/Stock-analyzer` ثم
  `claude`). هذا الملف ما ينقرأ إلا كذا، وما في أي مكان ثاني يذكّر كلود
  بوجود المشروع أصلاً.

---

# 2. المواصفات

### شروط الفلترة (لازم الأربعة تتحقق مع بعض)

1. القيمة السوقية بين 40 و 60 مليون دولار
2. الفلوت (Float) أكثر من 500,000 سهم
3. قفزة بحجم التداول أعلى من المعدل الطبيعي (النسبة تُحدد تجريبياً)
4. السعر فوق VWAP

### منطق التنبيه

- **سعر الدخول** = السعر الحالي وقت تحقق الشروط
- **وقف الخسارة** = سعر الدخول − (ATR × مضاعف)، المضاعف يبدأ بين 1.5 و 2
- **بدون هدف ربح** حالياً
- رابط مباشر لشارت TradingView مع كل تنبيه

### المصادر والأدوات المتفق عليها

- **Finnhub** لبيانات السعر والحجم والتاريخي (الباقة المجانية)
- **Telegram Bot** للتنبيهات
- **REST API** وليس WebSocket — أخف على بطارية الجوال
- **تخزين بملف JSON** بسيط، بدون قاعدة بيانات
- **بايثون أساسي**، بدون مكتبات ثقيلة مثل pandas أو numpy في البداية

### ملاحظات على بيانات Finnhub (مهمة قبل المرحلة 3)

- **`marketCapitalization` بالمليون، مو بالدولار.** يعني آبل ترجع
  `4572794` = 4.57 تريليون دولار. شرط الفلترة عندنا "بين 40 و 60 مليون
  دولار" معناه القيمة بين **40 و 60** بالضبط في رد فينهب — مو 40,000,000.
  هذا أسهل غلط ممكن يصير في المرحلة 3 ويخلي الفلتر ما يطلّع ولا سهم.
- **الرمز الغلط ما يرجّع خطأ** — يرجّع أصفار في كل الحقول. لازم نفحص القيمة
  بنفسنا، وهذا معمول في `get_quote()`.
- بيانات القيمة السوقية والفلوت تجي من `stock/profile2`، والسعر من `quote`.
- **الحد: 60 طلب في الدقيقة** (مقروء من ترويسة `x-ratelimit-limit` في الرد
  نفسه). فحص 100 سهم ≈ 200 طلب ≈ 3-4 دقائق للفحصة الوحدة.

**الباقة المجانية تحجب البيانات التاريخية (`stock/candle` يرجّع 403).**
اختُبر فعلياً 2026-08-10. وهذا يعطّل ATR و VWAP وحجم اليوم.

## مصدرين، كل واحد لدوره (2026-08-10)

النقص أعلاه انحل بإضافة **Twelve Data** كمصدر ثاني مجاني — يغطي كل شي
Finnhub يحجبه. جدول المقارنة الكامل في `README.md`.

**كل الشروط الأربعة + ATR مغطّاة الآن.** انتُهي منها 2026-08-10 باختبار
بمفاتيح حقيقية على الباقتين المجانيتين. الجدول الكامل في `README.md`.

| المطلوب | المصدر |
|---|---|
| شرط 1 — القيمة السوقية | **Finnhub** (`profile2`) |
| شرط 2 — الفلوت | **Finnhub** (`profile2` → `floatingShare`) |
| شرط 3 — قفزة الحجم | **Twelve Data** (`quote` → `volume` + `average_volume`) |
| شرط 4 — VWAP | **Twelve Data** (`vwap`، يومي أو `5min`) |
| المرحلة 4 — ATR | **Twelve Data** (`atr`، محسوب جاهز) |

**ما في مصدر واحد يكفي** — كل واحد يغطي بالضبط اللي الثاني يحجبه:
`stock/candle` محجوب عند Finnhub، و `statistics` محجوب عند Twelve Data.
فـ **Finnhub يبني القائمة، و Twelve Data يفحصها**.

### قواعد تقنية لازم تُحترم في المرحلة 3

1. **الأرصدة تُحسب لكل رمز، مو لكل طلب** (من ترويسة `api-credits-request`:
   3 رموز = 3 أرصدة). التجميع `symbol=A,B,C` يوفّر من حد الـ 8/دقيقة، مو من
   الـ 800/يوم.
2. **رتّب الطلبات من الأرخص للأغلى:** `quote` مجمّعة للكل أول، وبعدها `vwap`
   للأسهم اللي نجحت في شرط الحجم بس. الأغلب يسقط قبلها.
3. **بيانات Finnhub (قيمة سوقية + فلوت) ما تتغير كل ساعة** — تُجلب مرة في
   اليوم وتُخزّن، مو مع كل فحصة.
4. **فرق الوحدات — أسهل غلط ممكن يصير:** Finnhub يرجّع القيمة السوقية
   **بالمليون** (آبل = `4572794`). فشرط "40–60 مليون" = الرقم بين `40` و
   `60` بالضبط.
5. **لا تثق بمفتاح تجريبي.** مفتاح `demo` عند Twelve Data كان يفتح
   `statistics` عادي، والمفتاح المجاني الحقيقي رفضه بـ 403. اختبر دايماً
   بالمفتاح اللي بيشتغل به البوت فعلاً.

### قيود البنية

- كل الوظائف داخل **سكربت بايثون واحد** (`bot.py`)، مو ملفات منفصلة
- إعدادات Termux المطلوبة لاحقاً: `termux-wake-lock`، استثناء التطبيق من
  توفير البطارية، و Termux:Boot لإعادة التشغيل التلقائي

---

# 3. ترتيب البناء

هذا الجدول هو **المصدر الوحيد لحالة المشروع**. تفاصيل كل مرحلة في `SESSIONS.md`.

| # | المرحلة | الحالة |
|---|---|---|
| 1 | بوت تيليجرام بسيط يرسل رسالة تجريبية عند التشغيل | ✅ خلصت ومجرّبة على الجوال 2026-08-10 |
| 2 | ربط Finnhub وجلب سعر سهم واحد وإرساله على تيليجرام | ✅ خلصت ومجرّبة على الجوال 2026-08-10 |
| 3 | تطبيق الفلترة على قائمة الأسهم | ✅ خلصت 2026-08-10 — **القائمة تُبنى آلياً من السوق كله، مو يدوياً** |
| 4 | إضافة حساب ATR ووقف الخسارة داخل التنبيه | ✅ خلصت 2026-08-10 |
| 5 | بوت إدارة المخاطر (يحتاج حساب تداول فعلي) | ⬜ **آخر شي — تحتاج قرار منه قبل ما نبدأ** |

**تغيير على المرحلة 3 عن الخطة الأصلية:** الخطة كانت "قائمة 50-100 سهم
تُجهّز يدوياً". بُنيت **آلياً** بدلاً عن ذلك، لأن صاحب المشروع ما يقدر يعرف
بنفسه أي أسهم قيمتها السوقية بين 40 و 60 مليون — هذي بيانات ما تنكتب يدوياً.
`/refresh` يمشي على السوق الأمريكي كله (**٥-٦ ساعات، رقم حقيقي مقاس
2026-08-11 — كان موثّق خطأ كـ"ساعة ونص" قبل القياس الفعلي**) ويطلّع القائمة.

**إعدادات الفلترة قابلة للتعديل من `config.json`** (الافتراضيات في `DEFAULTS`
داخل `bot.py`):

| الإعداد | الافتراضي |
|---|---|
| `min_market_cap_musd` / `max_market_cap_musd` | 40 / 60 مليون |
| `min_float_shares` | 500,000 |
| `volume_spike_ratio` | 2.0 — **تجريبي، يتعدّل بعد ما نشوف نتائج حقيقية** |
| `scan_interval_minutes` | 15 |
| `atr_multiplier` | 1.75 — حسب المواصفات يبدأ بين 1.5 و 2 |
| `max_watchlist_size` | 50 في الكود، لكن **مُعدّل لـ 30 في `config.json` على الجوال** (قراره 2026-08-12، بند 18 في `SESSIONS.md`) — 30 × 26 فحصة/يوم ≈ 780 رصيد Twelve Data، تحت حد 800 بهامش ضيق |

**وقف الخسارة (المرحلة 4):** `compute_stop_loss()` يجيب ATR (يومي، 14 فترة)
من Twelve Data ويحسب `سعر الدخول − (ATR × المضاعف)`. **رصيد إضافي واحد لكل
تنبيه فقط** — يشتغل بعد ما الشروط الأربعة تتحقق، وهذي نادرة، فما يأثر على
الميزانية اليومية.

**لو فشل طلب ATR، التنبيه يوصل بدون ما يسقط بالكامل** — قرار مقصود: تنبيه
بدون وقف خسارة أفضل من ولا تنبيه، والرسالة توضّح النقص وتطلب مراجعة يدوية.

**رابط TradingView مضاف لكل تنبيه** (`tradingview_url()`) — كان في المواصفات
الأصلية من البداية، ما كان مبني قبل الآن.

---

# 3ج. مشاركة البوت مع صديقه (قراره، 2026-08-11)

**قراره صراحة:** نفس البوت الحالي يخدم الاثنين (نفس التوكن)، مو بوت مستقل
للصديق. **طلب اثنين آخرين معها (`/setlist` وأهداف الربح) بُنيا في نفس
الجلسة ثم طُلب التراجع عنهما — "لا تطبق هذه التعديلات إلا لمن أقلك، خلي كل
شي زي ما هو ما عدا مشاركة البوت".** رجعا بالكامل من الكود، ولا وجود لهما.

**مؤجلان، مو ملغيان — أكّد هو نفسه (2026-08-11):** "التعديل اللي طلبته منك
اعتبره فورك [محجوز]، لا تنساه، بعدين بنطبقه." يعني: **لا تُعاد بناؤهما
تلقائياً**، لكنهما باقيان كطلب معلّق ينتظر إشارته — مو فكرة مرفوضة أو
منسية. لو رجع يذكرهما، الكود القديم في تاريخ git (commit قبل التراجع) نقطة
بداية جاهزة، مو لازم يُبنى من الصفر.

- **`telegram_chat_ids` (قائمة) في `config.json`** — الجديد. لو موجودة تُستخدم
  بدل `telegram_chat_id` القديم. `allowed_chat_ids()` تتحقق من الاثنين
  (توافق خلفي — الإعداد الحالي على الجوال يشتغل بدون تعديل).
- **كل التنبيهات وتقدم `/refresh` تُبث للكل** عبر `broadcast()`. **ردود
  الأوامر (`/status` `/price` …) ترجع لطالبها بس**، مو للكل.
- **حالة `/mute` مشتركة** — لأنها في `state.json` واحد على الجهاز. لو
  وحد كتم، التنبيهات توقف للاثنين. لم يُطلب فصلها، ولا صلاحيات متفاوتة
  (owner-only لبعض الأوامر) — كل المصرح لهم متساوون بالصلاحيات.
- **مشكلة حقيقية حُلّت: كيف يعرف صاحب المشروع chat_id صديقه؟** لو صديقه راسل
  البوت وهو مو بالقائمة، **ما يرد عليه البوت** (أمان — ما يكشف تفاصيل
  للغريب)، لكن **يرسل لصاحب المشروع (أول رقم بالقائمة) تنبيه فيه اسم ورقم
  الشخص الجديد.** يضيفه بنفسه لـ `telegram_chat_ids` ويعيد التشغيل. بدون
  هذا، كان أول أمر يحتاج إيقاف البوت يدوياً وقراءة السجل.
- **رقم الهاتف لوحده ما يكفي لإضافة أحد.** بوتات تيليجرام ما تقدر تحوّل رقم
  هاتف إلى chat_id — هذا مقصود من تيليجرام نفسه لحماية الخصوصية، ومافي أي
  نقطة API تسمح بذلك. **الطريقة الوحيدة: الشخص يراسل البوت بنفسه** ولو
  مرة وحدة، وحينها آلية التبليغ أعلاه تكشف رقمه تلقائياً.

### أوامر الطرفية

```bash
./run.sh                 # يشغّل البوت مع الحارس (الطريقة المعتادة)
python bot.py run        # يشغّل البوت بدون حارس (للتجربة)
python bot.py            # رسالة تجريبية
python bot.py chatid     # يطبع رقم المحادثة
python bot.py price AAPL # سعر سهم من الطرفية
python test.py           # الاختبارات — تشتغل على الماك بدون جوال ولا إنترنت
```

للتشغيل بحيث يضل بعد ما تسكّر SSH:
`setsid nohup ./run.sh >/dev/null 2>&1 </dev/null &`

---

# 3ب. آلية التخاطب مع البوت (قراره، 2026-08-10)

**البوت يسمع أوامرك ٢٤ ساعة، ويفحص الأسهم وقت السوق الأمريكي بس.**

هذا الفصل مقصود: السماع لتيليجرام **مجاني** (ما يستهلك أرصدة البيانات)،
والفحص هو اللي يصرف. ولو قفلنا السماع بعد السوق، **البوت الصامت والبوت الميت
شكلهم واحد** — وهذي بالضبط المشكلة اللي `/status` موجود عشانها.

### الأوامر (بالإنجليزي — قراره)

| الأمر | الحالة |
|---|---|
| `/help` | ✅ مبني |
| `/status` | ✅ مبني — البوت والسوق والتوقيتين |
| `/price AAPL` | ✅ مبني |
| `/mute` و `/unmute` | ✅ مبني — يوقف التنبيهات، **مو البوت** |
| `/list` | ✅ مبني — يعرض قائمة المراقبة |
| `/refresh` | ✅ مبني — يبني القائمة من السوق كله (شغل خلفي ٥-٦ ساعات، حفظ تدريجي) |
| `/restore` | ✅ مبني — يرجّع القائمة اللي قبل آخر `/refresh` من نسخة احتياطية |
| `/scan` | ✅ مبني — فحص فوري |
| `/setlist` | ⏸️ **مؤجل بطلبه، مو مبني** — راجع §3ج |

**القائمة في تيليجرام (الزر الأزرق) تتبني من نفس قائمة `COMMANDS` في الكود**،
وتُسجّل عند تيليجرام مع كل تشغيل. أي أمر جديد ينضاف في مكان واحد، وما يصير
اختلاف بين القائمة و `/help`.

**المهام الطويلة تشتغل في خيط منفصل** (`run_in_background`) عشان `/refresh`
اللي ياخذ ساعات ما يجمّد باقي الأوامر. و `BUSY` يمنع تشغيل نفس المهمة
مرتين مع بعض.

**⚠️ لا تعيد تشغيل البوت وهو وسط `/refresh` بدون داعي.** المهمة تشتغل في
خيط داخل نفس العملية — إعادة تشغيل العملية (`kill` أو حتى إعادة نشر كود)
تقتل الخيط فوراً، ولا شي يعيد تشغيل الفحص من عنده. **`BUSY.get("refresh")`
يبيّن لو فيه فحص شغّال قبل ما تلمس العملية.** الحفظ التدريجي (أسفل) يمنع
خسارة اللي اتلقى، لكن الفحص نفسه لازم يبدأ من الصفر بعد أي انقطاع.

### حادثة حقيقية 2026-08-11: إعادة تشغيل مسحت فحص وصل لمنتصفه

فحص `/refresh` وصل 1500/18424 ولقى 25 سهم (~45 دقيقة شغل)، وانت أعدت تشغيل
البوت (لنشر كود آخر — مو غلط منك، الخطأ إن الكود ما كان يحمي من هذا). القائمة
ما كانت تُحفظ إلا في نهاية `build_watchlist()`، فكل الـ25 سهم راحوا، ولا
طريقة لاسترجاعهم (الأسماء نفسها ما كانت محفوظة بأي مكان، حتى رسائل التقدم
في تيليجرام كانت عدّاد بس، مو قائمة رموز).

**الإصلاح — جزءين:**
1. **`build_watchlist()` يحفظ `save_watchlist(found)` فور كل سهم جديد يدخل
   القائمة**، مو مرة وحدة في النهاية. حادثة زي هذي بعدها تخسر أقصى شي دقيقة
   واحدة من الشغل، مو 45 دقيقة.
2. **تسريع الفحص:** الكود القديم كان يضيف ثانية `sleep(1)` **فوق** وقت
   الطلب نفسه (~0.7-0.8 ثانية)، فيعطي ~33-40 طلب/دقيقة فعلياً رغم إن حد
   Finnhub يسمح بـ60. الجديد يحسب الانتظار من **بداية الطلب السابق**، مو
   بعد نهايته، ويستهدف 55 طلب/دقيقة (هامش أمان تحت 60). هذا وحده قرّب
   المدة الكاملة من ~7.6 ساعة إلى ~5.6 ساعة.

**اختُبر:** `test.py` فيه اختبار مخصص (`build_watchlist يحفظ تدريجياً`) يتأكد
إن `save_watchlist` تُستدعى بعد كل سهم جديد، مو بس مرة بالنهاية، وإن القائمة
على القرص تطابق النتيجة النهائية.

### ⛔ حادثة ثانية 2026-08-11: `python test.py` على الجوال مسح قائمة 335 سهم

**خطأ مني، مو منه.** بعد ما توقف فحص وصل 335 سهم (~4.5 ساعة شغل حقيقي)،
شغّلت `python test.py` على الجوال للتأكد من كود جديد. **الاختبارات كانت
تكتب وتحذف `bot.WATCHLIST_PATH` نفسه** — نفس الملف اللي البوت يعتمد عليه —
فمسحت القائمة بالكامل. ما كانت في نسخة احتياطية لأن الفحص ما اكتمل أصلاً.

**الإصلاح في `test.py` (لا يُشال أبداً):** كل مسارات البيانات
(`WATCHLIST_PATH`, `WATCHLIST_BACKUP_PATH`, `STATE_PATH`) تتحوّل لمجلد مؤقت
عبر `tempfile.mkdtemp()` **قبل** أي اختبار. وأضيف **اختبار حارس يشتغل أول
شي** ويتأكد إن ولا مسار منها يشير لمجلد المشروع — لو صار، يوقف فوراً بـ
`sys.exit(1)` قبل ما يمس أي ملف.

**القاعدة المستفادة:** أي اختبار يكتب ملفات لازم يُعزل عن بيانات الإنتاج
**قبل** ما يُشغّل على جهاز فيه بيانات حية. تشغيل الاختبارات على الجوال صار
آمناً الآن، لكن الأصل تشغيلها على الماك.

### النسخة الاحتياطية + `/restore` (طلبه، بعد الحادثة مباشرة)

الحادثة أعلاه كشفت خطر ثانٍ: `/refresh` **يستبدل القائمة بالكامل من الصفر**
حتى لو ما فيه انقطاع — أول سهم يطابقه الفحص الجديد يمحي القائمة القديمة
اللي كانت شغّالة، حتى لو كانت أفضل. طلب صراحة: "احفظ نسخة من آخر قائمة...
يرجعلها إذا ما عجبته القائمة [الجديدة]."

- **`backup_current_watchlist()`** — يُستدعى مرة وحدة في `start_refresh()`
  **قبل** ما الخيط يبدأ، لا داخل `build_watchlist()`. ينسخ `watchlist.json`
  الحالي (لو فيه شي) إلى `watchlist.backup.json`. **نسخة وحدة، مو تاريخ
  كامل** — كل `/refresh` جديد يستبدل النسخة الاحتياطية بالقائمة اللي كانت
  شغّالة مباشرة قبله.
- **`/restore`** — يرجّع `watchlist.json` لمحتوى `watchlist.backup.json`.
  لو ما في نسخة (أول تشغيل على الإطلاق)، يقول كذا بوضوح بدل ما ينهار.
  محمي بنفس `BUSY.get("refresh")` — ما يشتغل والفحص شغّال.
- **رسالة نهاية `/refresh` تذكّر بـ `/restore`** صراحة، عشان ما يحتاج
  يحفظها من ذاكرته.
- كلاهما في `.gitignore` (`watchlist.backup.json`) — نفس منطق
  `watchlist.json`، بيانات محلية مو مصدر كود.
- **اختُبر (3 حالات):** بدون قائمة قديمة ما ينسخ شي؛ قائمة قديمة تُحفظ حتى
  بعد ما الأساسية تتغيّر؛ `cmd_restore()` يرجّع الأساسية فعلياً للنسخة.

**`/mute` يوقف التنبيهات بس، ما يوقف البوت** — مقصود. لو أوقفنا البوت من
تيليجرام، ما راح يقدر يشغّله إلا من الجوال نفسه.

**`/status` يعرض حالة حظر Twelve Data** (لو نفدت الأرصدة اليوم) والوقت
المتبقي لتجددها — ✅ مبني 2026-08-12، راجع الحادثة تحت.

### ⚠️ حادثة 2026-08-11 + إصلاح 2026-08-12: نفاد أرصدة Twelve Data كان يخلي الفحص يعيد المحاولة ويفشل كل 15 دقيقة

نفدت أرصدة Twelve Data اليومية (800 مجاني) بدري، والفحص التلقائي استمر
يحاول كل `scan_interval_minutes` ويفشل، وكل محاولة فاشلة كانت تصرف رصيد
إضافي (سلبي) بدون فايدة — لين قفل السوق. التفاصيل الكاملة والإصلاح في
`SESSIONS.md` بند 16.

**الحالة `state["twelvedata_blocked_until"]` (ISO، بتوقيت UTC) — لازم يُحترم:**
أي كود جديد يلمس `start_scan()` أو منطق الفحص التلقائي **لازم** يتأكد من
هذا الحقل أول قبل أي اتصال بـ Twelve Data. القيمة تُحسب على **منتصف الليل
UTC** — **افتراض غير مؤكد بعد، مو حقيقة مُختبرة** (نفس الدرس اللي المشروع
وثّقه بنفسه: "لا تثق بمفتاح تجريبي ولا بصفحة أسعار — اختبر بالمفتاح اللي
بيشتغل به البوت فعلاً"). لو تأكد لاحقاً إن التجدد الفعلي بتوقيت ثاني، لازم
`next_utc_midnight()` تتعدّل. **تأكيد ميداني:** أول سجل بعد 2026-08-12
يوضّح متى بالضبط رجع أول فحص ينجح بدون خطأ — قارنه بالفرضية. الفشل هنا
متسامح بالاتجاهين (فتح بدري = فحصة وحدة فاشلة تعيد ضبط الحظر، فتح متأخر =
خسارة وقت فحص بس) فما يستاهل يوقف الشغل، بس لازم يُتأكد منه، مو يُفترض.
`is_credit_exhausted()` هي نقطة الفحص الوحيدة لتمييز رسالة "نفدت الأرصدة"
عن أي خطأ Twelve Data ثاني — لا تكرر منطق مطابقة نص الخطأ في مكان جديد
(مغطّاة في مرحلتي `quote` و `vwap` داخل `scan_watchlist()`، الاثنتين).

**مهم: `/refresh` (`build_watchlist()`) ما يلمس Twelve Data إطلاقاً** — يبني
القائمة من Finnhub بس (`profile2` للقيمة السوقية والفلوت). حظر
`twelvedata_blocked_until` **ما يوقف `/refresh` ولا يأخره** — الاثنان
مسارين منفصلين تماماً. لا تفترض إن نفاد أرصدة Twelve Data يعني الانتظار
قبل تشغيل `/refresh`.

### الحارس `run.sh` — الاستثناء الوحيد لقاعدة الملف الواحد

قاعدة المشروع "كل الوظائف في `bot.py`" لسه سارية. `run.sh` استثناء لسبب
تقني: **البرنامج ما يقدر يعيد تشغيل نفسه بعد ما ينقتل**، لازم شي بره يراقبه.

**مُختبر فعلياً على الجوال 2026-08-10** بـ `kill -9` حقيقي: البوت مات (كود
137)، والحارس رصده وأعاد تشغيله بعد 10 ثواني.

### ⚠️ فخّ: تشغيل شي في الخلفية عبر SSH يعلّق الاتصال

`setsid nohup ./run.sh &` داخل أمر SSH **يخلي SSH ينتظر للأبد** لأن العملية
الجديدة تمسك قناة الاتصال. صار ثلاث مرات في جلسة واحدة.

**الطريقة اللي تشتغل — أمرين منفصلين:**

```bash
# 1) وقّف القديم
ssh phone 'pkill -f "[r]un.sh"; pkill -f "[b]ot.py run"'

# 2) شغّل الجديد، وأغلق كل القنوات الثلاث
ssh phone 'cd ~/stock-analyzer && setsid ./run.sh >/dev/null 2>&1 </dev/null & exit 0'

# 3) تحقق باتصال جديد
ssh phone 'pgrep -f "[b]ot.py run" && echo شغّال'
```

`</dev/null` جزء أساسي — بدونه العملية تمسك stdin وتعلّق الاتصال.

### ⚠️ فخّ عملي: `pgrep -f` و `pkill -f` يطابقون نفسهم عبر SSH

الأمر اللي ترسله عبر SSH يحتوي النص اللي تبحث عنه، فيطابق نفسه —
و `kill` يقتل جلستك أنت. **صار فعلاً في هذي الجلسة.**

الحل: حيلة القوسين، تكسر المطابقة بدون ما تغيّر النتيجة:

```bash
pgrep -f "[b]ot.py run"     # صح
pgrep -f "bot.py run"       # غلط — يطابق أمر SSH نفسه
```

---

# 4. طريقة الشغل — الجوال + SSH

البوت **يشتغل على الجوال**، والتعديل يصير على الماك، والنقل بينهم عن طريق
GitHub — مو نسخ ولصق.

| الجهاز | دوره |
|---|---|
| الماك | الكود الأصلي: `~/Documents/Stock-analyzer/` — هنا التعديل والـ commit والـ push |
| الجوال | التشغيل: `~/stock-analyzer` داخل Termux — يستقبل التحديثات بـ `git pull` فقط |

**ليش ما نعدّل على الجوال:** الكود اللي يتكتب على الجوال بس، له نسخة وحدة ما
لها احتياطي — يضيع مع الجوال. اللي على GitHub محفوظ ومنقول لأي جهاز.

**دورة التحديث الكاملة:**

```bash
# على الماك بعد أي تعديل
git add -A && git commit -m "..." && git push

# على الجوال (كلود ينفذها بنفسه عبر SSH)
cd ~/stock-analyzer && git pull
```

### الاتصال بالجوال

من الماك مباشرة: `ssh phone`. كلود يشغّل ويختبر على الجهاز الحقيقي بنفسه —
ما يطلب من صاحب المشروع ينسخ أوامر ولا يصوّر شاشة.

اختصار `phone` معرّف في `~/.ssh/config` على الماك (إعداد جهاز، مو ملف مشروع،
عشان كذا ما ينرفع على GitHub). تفاصيله لو احتاج يُعاد ضبطه:

```
Host phone
    HostName 192.168.8.232     # يتغيّر لو الراوتر أعطى الجوال رقم ثاني
    Port 8022                  # منفذ Termux، مو 22
    User u0_a330               # اسم المستخدم داخل Termux
    IdentityFile ~/.ssh/<مفتاحك>
```

**لو ما نفع `ssh phone`:** غالباً خدمة SSH على الجوال واقفة — افتح Termux
واكتب `sshd`. ولو تغيّر رقم IP الجوال، حدّثه في `~/.ssh/config` على الماك.

---

# 5. النقل لجهاز ثاني

**المشروع بينتقل لجوال صديق صاحب المشروع لما يخلص.** معناها كل شي لازم يكون
على GitHub، وما يعتمد على هالجوال بالذات. قاعدتين تحمي هذا الهدف:

1. **أي ملف جديد أو تعديل → commit و push فوراً.** اللي يبقى على الجوال بس،
   ما ينتقل مع المشروع.
2. **`README.md` لازم يبقى كافي لوحده** — أي شخص يفتحه على جوال نظيف يشغّل
   المشروع بدون ما يسأل أحد. أي خطوة إعداد جديدة تنضاف له فوراً.

**الشي الوحيد اللي ما ينتقل: `config.json`** — فيه توكن البوت وهو خاص. صديقه
يسوي بوت خاص فيه من BotFather ويعبّي ملفه بنفسه، والخطوات في README.

**الريبو حالياً عام (public)**، يعني صديقه يسحبه مباشرة بدون صلاحيات. لو صار
خاص (private) لاحقاً، لازم يُعطى صديقه **والجوال** صلاحية وصول، وإلا
`git clone` و `git pull` بيرفضون.

---

# 6. قواعد الأمان

- التوكنات ومفاتيح API تروح في `config.json` فقط، وهو في `.gitignore`.
- **لا تكتب أي توكن أو مفتاح داخل الكود** ولا في أي ملف يُرفع على GitHub.
- مفتاح Finnhub في المرحلة 2 يتحط في `config.json` بنفس الطريقة.
- قبل أي `push`: تأكد إن `git status` ما يشوف `config.json`.

---

# 7. Git

- الريبو: `alpha2xyz/Stock-analyzer`
- الفرع: `claude/new-project-v4uayu` — **ما في فرع `main` في هذا الريبو**

---

# 8. التقرير المرئي — يتحدّث كل جلسة

**الرابط: https://claude.ai/code/artifact/8fc2cf74-cb7d-4c19-a982-47570485a4e8**

صفحة عربية تلخّص حالة المشروع بصرياً. مصدرها `report.html` في هذا المجلد،
فهي تنتقل مع المشروع مثل باقي الملفات.

**قاعدتين لازم تُتبعان:**

1. **حدّثه قبل نهاية أي جلسة تغيّر فيها شي حقيقي** — مرحلة خلصت، أو نتيجة
   اختبار جديدة، أو قرار انتخذ. التقرير يوصف **الوضع الحالي**، مو تاريخ
   التغييرات: الشي المحذوف ينشال منه، والشي اللي تغيّر ينكتب بصيغته الجديدة
   بس.
2. **مرّر الرابط نفسه كـ `url` عند النشر.** أي جلسة تنشر بدونه بتولّد رابط
   جديد، والرابط القديم يبقى قديم عند صاحب المشروع.

**ممنوع يدخل التقرير:** أي توكن أو مفتاح API. الصفحة تُنشر على الإنترنت.
