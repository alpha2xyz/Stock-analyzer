# مشروع بوت تنبيهات الأسهم الأمريكية

## قواعد التعامل مع صاحب المشروع (مهمة جداً)

- صاحب المشروع **مبتدئ تماماً بالبرمجة**. اشرح كل خطوة بلغة بسيطة.
- **لا تفترض** إنه يفهم أي مصطلح تقني بدون ما توضحه.
- **اسأله قبل تثبيت أي مكتبة جديدة** — لا تثبت شي من نفسك.
- **لا تقفز مراحل.** رتّب الشغل حسب "ترتيب البناء" تحت، مرحلة مرحلة.
- التواصل بالعربي.

## الفكرة العامة

بوت بايثون يشتغل على جوال أندرويد عن طريق Termux، يفحص السوق الأمريكي بشكل
دوري، يطبّق شروط فلترة، ولما سهم يحقق الشروط يرسل تنبيه على تيليجرام فيه
سعر دخول مقترح ووقف خسارة محسوب ورابط شارت TradingView.

هذا **مشروع مستقل بالكامل**. ما له أي ارتباط أو إشارة لأي مشروع أو مجلد
ثاني على نفس الجهاز. لا تربطه بأي شي خارجي.

## شروط الفلترة (لازم الأربعة تتحقق مع بعض)

1. القيمة السوقية بين 40 و 60 مليون دولار
2. الفلوت (Float) أكثر من 500,000 سهم
3. قفزة بحجم التداول أعلى من المعدل الطبيعي (النسبة تُحدد تجريبياً)
4. السعر فوق VWAP

## منطق التنبيه

- **سعر الدخول** = السعر الحالي وقت تحقق الشروط
- **وقف الخسارة** = سعر الدخول − (ATR × مضاعف)، المضاعف يبدأ بين 1.5 و 2
- **بدون هدف ربح** حالياً
- رابط مباشر لشارت TradingView مع كل تنبيه

## المصادر والأدوات المتفق عليها

- **Finnhub** لبيانات السعر والحجم والتاريخي (الباقة المجانية)
- **Telegram Bot** للتنبيهات
- **REST API** وليس WebSocket — أخف على بطارية الجوال
- **تخزين بملف JSON** بسيط، بدون قاعدة بيانات
- **بايثون أساسي**، بدون مكتبات ثقيلة مثل pandas أو numpy في البداية

## قيود البنية

- كل الوظائف داخل **سكربت بايثون واحد** (`bot.py`)، مو ملفات منفصلة
- إعدادات Termux المطلوبة لاحقاً: `termux-wake-lock`، استثناء التطبيق من
  توفير البطارية، و Termux:Boot لإعادة التشغيل التلقائي

## طريقة الشغل — الجوال + SSH (اتُفق عليها 2026-08-10)

البوت **يشتغل على الجوال**، والتعديل والحفظ يصير على الماك. التوزيع بينهم
عن طريق GitHub، مو نسخ ولصق.

| الجهاز | دوره |
|---|---|
| الماك | مكان الكود الأصلي: `~/Documents/Stock-analyzer/` — هنا يتم التعديل والـ commit والـ push |
| الجوال | مكان التشغيل: `~/stock-analyzer` داخل Termux — يستقبل التحديثات بـ `git pull` فقط |

**ليش كذا:** أي كود يتكتب على الجوال بس ويصير له شي، يروح ولا له نسخة ثانية.
الكود اللي على GitHub محفوظ دايماً.

**الاتصال بالجوال:** من الماك مباشرة بأمر `ssh phone` (الإعداد محفوظ في
`~/.ssh/config` على الماك، خارج المشروع). كلود يشغّل ويختبر على الجهاز
الحقيقي بنفسه — ما يحتاج يطلب من صاحب المشروع ينسخ أوامر أو يصوّر الشاشة.

**دورة التحديث الكاملة:**

```bash
# على الماك: بعد أي تعديل
git add -A && git commit -m "..." && git push

# على الجوال (كلود ينفذها عبر SSH)
cd ~/stock-analyzer && git pull
```

**ملاحظة مهمة عن `config.json`:** موجود على الجوال فقط، ما ينرفع على GitHub
أبداً، و `git pull` ما يمسّه.

**لو ما نفع `ssh phone`:** غالباً خدمة SSH على الجوال واقفة. الحل: افتح
Termux على الجوال واكتب `sshd`. وإذا تغيّر رقم IP الجوال، يتحدّث في
`~/.ssh/config` على الماك.

## ترتيب البناء

| # | المرحلة | الحالة |
|---|---|---|
| 1 | بوت تيليجرام بسيط يرسل رسالة تجريبية عند التشغيل | ✅ خلصت |
| 2 | ربط Finnhub وجلب سعر سهم واحد وإرساله على تيليجرام | ⬜ التالية |
| 3 | تطبيق الفلترة على قائمة 50-100 سهم (تُجهّز يدوياً بالبداية) | ⬜ |
| 4 | إضافة حساب ATR ووقف الخسارة داخل التنبيه | ⬜ |
| 5 | بوت إدارة المخاطر (يحتاج حساب تداول فعلي) | ⬜ آخر شي |

## وضع المشروع حالياً

المرحلة 1 مكتملة ومرفوعة. الملفات:

| الملف | الوظيفة |
|---|---|
| `bot.py` | كل الكود. حالياً يرسل رسالة تجريبية فقط |
| `config.example.json` | نموذج الإعدادات (مرفوع على GitHub) |
| `config.json` | الإعدادات الحقيقية بالتوكن — **في .gitignore، لا يُرفع أبداً** |
| `README.md` | شرح التنصيب والتشغيل خطوة بخطوة |

في المرحلة 1 **صفر مكتبات خارجية** — الكود يستخدم مكتبة `urllib` القياسية
فقط، فما يحتاج أي `pip install`.

أوامر التشغيل:

```bash
python bot.py            # يرسل رسالة تجريبية
python bot.py chatid     # يطبع رقم المحادثة (chat_id)
```

## قواعد الأمان

- التوكنات ومفاتيح API تروح في `config.json` فقط، وهو في `.gitignore`.
- **لا تكتب أي توكن أو مفتاح داخل الكود** ولا في أي ملف يُرفع على GitHub.
- لما نضيف مفتاح Finnhub في المرحلة 2، يتحط في `config.json` بنفس الطريقة.

## Git

- الريبو: `alpha2xyz/Stock-analyzer`
- فرع التطوير: `claude/new-project-v4uayu`
