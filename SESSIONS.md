# سجل الجلسات

سجل الشغل على هذا المشروع. **هذا الملف هو الذاكرة الوحيدة للمشروع** — ما في
أي ملاحظات عنه في أي مكان ثاني على الجهاز. كل جلسة تضيف قسم جديد هنا،
وينرفع على GitHub مع باقي الملفات.

---

## 2026-08-10 — تشغيل المشروع على الجوال عن طريق SSH

**النتيجة باختصار: المرحلة 1 خلصت ومجرّبة على الجوال الحقيقي — رسالة تجريبية
وصلت فعلاً على تيليجرام.**

### طريقة الشغل اللي اتفقنا عليها

الكود يتعدّل على الماك، ويشتغل على الجوال، والنقل بينهم عن طريق GitHub.

**ليش ما نعدّل على الجوال مباشرة؟** لأن أي كود يتكتب على الجوال بس، يصير له
نسخة وحدة ما لها احتياطي — لو ضاع الجوال أو انمسح Termux، راح الشغل كله.
الكود اللي على GitHub محفوظ دايماً، وينسحب على أي جهاز.

### وش انعمل بالضبط

- الجوال فيه Python 3.14.6 و git 2.55.0 جاهزين من قبل، ما احتاج تثبيت شي.
- انسحب المشروع على الجوال في `~/stock-analyzer` على الفرع الصحيح.
- أُنشئ البوت `@Stock7oot_bot`، والتوكن انكتب في `config.json` **على الجوال
  فقط** — ما مرّ على الماك ولا انرفع على GitHub، والتحقق منه تم مباشرة مع
  تيليجرام.
- `chat_id` انقرأ وانكتب، والبوت اشتغل ووصلت الرسالة.
- تم التأكد إن `git` ما يشوف `config.json` نهائياً (محمي في `.gitignore`).

### قرارات وملاحظات مهمة

- **الريبو حالياً عام (public).** هذا اللي يخلي الجوال يسحب التحديثات بدون
  أي كلمة سر، وهو نفسه اللي بيخلي صديق صاحب المشروع يسحبه بسهولة وقت النقل.
  لو صار خاص (private) لاحقاً، لازم صلاحية وصول للجوال ولصديقه.
- **ما في فرع `main`.** كل الشغل على الفرع `claude/new-project-v4uayu`، وهو
  الفرع الأساسي للريبو حالياً. ما تم تغييره، هذا قرار صاحب المشروع.
- على الجوال في مجلد فاضي قديم اسمه `stock-alert-bot` من محاولة سابقة —
  متروك بدون حذف لين يجي القرار.
- **مؤجل للمرحلة 3:** لما يصير في فحص دوري مستمر، بنحتاج طريقة تعيد تشغيل
  البوت تلقائياً، لأن هذا الجوال يقتل العمليات الطويلة. ما نبنيها قبل وقتها.

---

## 2026-08-10 (تكملة) — فصل المشروع عن باقي الجهاز + المرحلة 2

### 1. المشروع صار مغلق على نفسه — قراره

طلب إن ما يشوف للمشروع أي أثر في باقي مشاريعه. كل الملاحظات اللي كانت
مكتوبة عنه خارج هذا المجلد **انحذفت**، والمحتوى انتقل هنا:

| كان وين | صار وين |
|---|---|
| سطر في خريطة المشاريع العامة | انحذف |
| ملفين ذاكرة + سطرين في فهرس الذاكرة | انحذفوا، والمحتوى في `SESSIONS.md` |
| قسم في سجل النشاط العام | انحذف، والمحتوى في `SESSIONS.md` |
| استثناء اللغة العربية في إعداداته العامة | انحذف، والقاعدة صارت في `CLAUDE.md` هنا |

**النتيجة المهمة:** ما عاد في أي شي خارج هذا المجلد يذكّر كلود بوجود
المشروع. عشان كذا **لازم تُفتح الجلسة من داخل مجلد المشروع** — مكتوبة في
`CLAUDE.md`.

### 2. ترتيب ملفات المشروع

`CLAUDE.md` كان صار فيه تكرار وترتيب مبعثر، فانعاد كتابته كمرجع واحد مرتب
(مواصفات → قواعد → ترتيب البناء → طريقة الشغل → النقل → أمان). وانضاف
`SESSIONS.md` كذاكرة للمشروع، والملفات كلها صارت مذكورة في جدول واحد.

### 3. المرحلة 2 خلصت ومجرّبة على الجوال ✅

انضاف أمر `python bot.py price AAPL` — يجيب السعر من Finnhub ويرسله على
تيليجرام. **بدون أي مكتبة جديدة**، نفس `urllib` القياسية.

تجارب فعلية على الجوال، كلها نجحت:

- رمز صحيح (`AAPL`) → الرسالة وصلت
- رمز غلط (`NOTREAL`) → رسالة خطأ واضحة بدل ما ينهار
- بدون رمز → يشرح الاستخدام الصحيح

**تفصيلة صغيرة لكن مهمة:** الأرقام السالبة كانت بتنقلب داخل النص العربي
(‎-2.43 تظهر 2.43-)، فانضافت علامة `LTR` قبل كل رقم في الرسالة.

### 4. أهم اكتشاف في الجلسة: حدود الباقة المجانية

بدل ما نعتمد على كلام الموقع، انختبرت نقاط Finnhub وحدة وحدة بالمفتاح
الحقيقي. **النتيجة تغيّر خطة المرحلتين 3 و 4:**

- `stock/candle` (البيانات التاريخية) **محجوب — 403**. وهذا اللي يعطّل
  حساب ATR و VWAP وحجم تداول اليوم.
- الحد **60 طلب في الدقيقة** (مقروء من ترويسة الرد نفسها).
- المتاح: السعر، القيمة السوقية، الفلوت، متوسط الحجم، وقائمة الأسهم
  الأمريكية كاملة.

يعني من شروط الفلترة الأربعة: اثنين جاهزين، واحد ناقص، وواحد مو ممكن.
الجدول الكامل في `README.md`.

**وهذا يوقف المرحلة 3 عند قرار صاحب المشروع** — تعديل الشروط، أو مصدر
بيانات ثاني، أو باقة مدفوعة.

### 5. تقرير مرئي للمشروع

انبنى تقرير عربي يلخّص حالة المشروع بصرياً:
**https://claude.ai/code/artifact/8fc2cf74-cb7d-4c19-a982-47570485a4e8**

مصدره `report.html` داخل المجلد، فينتقل مع المشروع. **قاعدة التحديث في
`CLAUDE.md` قسم ٨** — أهم نقطة فيها: مرّر الرابط نفسه عند إعادة النشر، وإلا
يتولّد رابط جديد ويبقى القديم قديم.

### 6. البحث عن مصدر ثاني مجاني — انحل النقص

بدل ما نغيّر شروط الفلترة عشان تناسب Finnhub، انبحث عن مصدر مجاني يغطي
الناقص. **الشروط الأربعة تبقى كما هي.**

**اللي انجرّب وانرفض:**

| المصدر | ليش انرفض |
|---|---|
| Yahoo Finance (غير رسمي) | رجّع `429 Too Many Requests` مباشرة |
| Stooq | صار عنده تحقق JavaScript ضد البوتات |

الاثنين مصادر غير رسمية فيها حماية ضد البوتات — **ما تصلح أساس لبوت يشتغل
لحاله على جوال**، لأنها تنكسر بدون إنذار.

**اللي نجح: Twelve Data.** يغطي كل شي كان ناقص، واتأكدنا بطلبات حقيقية:
`time_series` فيه الحجم، و `vwap` يشتغل يومي وداخل اليوم، و **`atr` محسوب
جاهز من عندهم** (يعني ما نحسبه على الجوال)، و `statistics` فيه القيمة
السوقية والفلوت.

**القرار المعماري: نستخدم المصدرين مع بعض** — Finnhub للمسح الواسع (60
طلب/دقيقة)، و Twelve Data للبيانات العميقة على الأسهم الناجية (8/دقيقة،
800/يوم). التفصيل في `README.md` و `CLAUDE.md`.

**الملاحظة اللي تخلي الخطة تنجح:** شرط القيمة السوقية ضيق جداً (40–60
مليون)، فالناجين قليلين، فحصتهم رخيصة. الشرط الضيق هو اللي يخلي الباقة
المجانية تكفي.

### 7. المفتاح الحقيقي كشف فخّاً — والتحذير كان في محله

انجاء مفتاح Twelve Data المجاني الحقيقي، وانعادت الاختبارات عليه.
**النتيجة اختلفت عن مفتاح `demo`:**

| النقطة | بمفتاح demo | بالمفتاح المجاني الحقيقي |
|---|---|---|
| `atr` / `vwap` / `time_series` | ✅ | ✅ نفس الشي |
| `statistics` (قيمة سوقية + فلوت) | ✅ | ❌ **403 — للباقات المدفوعة** |

**الدرس، وتكرر مرتين في يوم واحد:** لا تثق بنتيجة مفتاح تجريبي، ولا بكلام
صفحة الأسعار. اختبر بالمفتاح اللي بيشتغل به البوت فعلاً. لو انبنت المرحلة 3
على نتيجة `demo`، كانت بتنهار عند أول تشغيل حقيقي.

**النتيجة النهائية: المصدرين صاروا ضرورة، مو تحسين.** كل واحد يغطي بالضبط
اللي الثاني يحجبه — `stock/candle` محجوب عند Finnhub، و `statistics` محجوب
عند Twelve Data. **Finnhub يبني القائمة، و Twelve Data يفحصها.**

**اكتشاف يبسّط الشغل:** `quote` عند Twelve Data يرجّع `volume` و
`average_volume` مع السعر في **طلب واحد** — يعني شرط ٣ ما يحتاج شموع
تاريخية أصلاً.

**واكتشاف يخص الميزانية:** الأرصدة تُحسب **لكل رمز مو لكل طلب** (مقروء من
ترويسة `api-credits-request`: ٣ رموز = ٣ أرصدة). التجميع يوفّر من حد
الـ ٨/دقيقة، مو من الـ ٨٠٠/يوم.

**المفتاح محفوظ على الجوال في `config.json`** تحت `twelvedata_api_key`،
وانضاف للنموذج `config.example.json`.

### 8. الميزانية اليومية — كم سهم نقدر نغطي

سؤاله. الجواب إن في **ميزانيتين منفصلتين**، ووحدة بس هي القيد:

- **بناء القائمة (Finnhub):** طلب واحد لكل سهم — `profile2` يرجّع القيمة
  السوقية والفلوت مع بعض. ٣٬٦٠٠ سهم في الساعة، يعني **نقدر نمسح السوق
  الأمريكي كله (~١٠٬٠٠٠ سهم) في ٢٫٨ ساعة، مرة كل ليلة**.
- **الفحص المتكرر (Twelve Data):** ٨٠٠ رصيد/يوم هو **القيد الحقيقي**.

| حجم القائمة | فحصات/يوم | فحص كل |
|---|---|---|
| ١٠ | ~٧٠ | ٥ دقائق |
| ٢٠ | ~٣٦ | ١١ دقيقة |
| ٥٠ | ~١٤ | ٢٨ دقيقة |
| ١٠٠ | ~٧ | ٥٦ دقيقة |

**التوصية: ٢٠–٣٠ سهم.** عند ١٠٠ سهم يصير الفحص كل ساعة — بطيء جداً لتنبيه
مبني على قفزة حجم، القفزة بتخلص قبل ما نشوفها.

### 9. آلية التخاطب — انبنت واشتغلت على الجوال ✅

**قراراته:** الأوامر بالإنجليزي، والبوت يشتغل ويقفل مع وقت السوق الأمريكي،
والخيار (أ) — بوت يضل شغّال.

**الحجة اللي غيّرت الترجيح:** كان مائل للخيار (ب) (بوت يصحى كل ١٠ دقائق).
لكن إضافة "وقت السوق" خلّت (ب) بلا فايدة: **الخيارين يحتاجون نفس الشي** —
حلقة تشغّل البوت في وقت معيّن. فـ(ب) ما توفّر الجزء الصعب، بس تكلّف سرعة
الرد. **الحارس يجي مجاناً مع أي خيار.**

**تعديل اقترحته وقبله:** فصل السماع عن الفحص — **يسمع ٢٤ ساعة (مجاني)،
ويفحص وقت السوق بس (يصرف أرصدة)**. لأن البوت الصامت والبوت الميت شكلهم واحد،
وهذي بالضبط المشكلة اللي `/status` موجود عشانها.

**اللي انبنى:** `/help` `/status` `/price` `/mute` `/unmute`.
`/list` `/refresh` `/scan` مؤجلة للمرحلة 3 لأنها تحتاج القائمة.

**مشكلة حقيقية انكشفت قبل ما تنفجر:** الجوال **ما عنده قاعدة بيانات المناطق
الزمنية** — `zoneinfo` رمى `ZoneInfoNotFoundError` على `America/New_York`.
الحل بدون تثبيت أي شي: حساب التوقيت الصيفي الأمريكي بأنفسنا (قاعدة ثابتة
بقانون من 2007: ثاني أحد مارس → أول أحد نوفمبر). **`test.py` فيه ٢٠ اختبار**
يغطون حدود التحويل والافتتاح والإغلاق بالدقيقة — لأن غلط ساعة واحدة هنا ما
ينكشف إلا بعد شهور.

**`run.sh` — استثناء وحيد لقاعدة الملف الواحد.** البرنامج ما يقدر يعيد
تشغيل نفسه بعد ما ينقتل. **مُختبر بـ `kill -9` حقيقي على الجوال**: البوت مات
(كود 137)، الحارس رصده وأعاده بعد ١٠ ثواني.

**فخّ تقني وقعت فيه فعلاً:** `pgrep -f "bot.py run"` عبر SSH **يطابق أمر
SSH نفسه**، فـ`kill` قتل جلستي. الحل حيلة القوسين `[b]ot.py run`. مسجّل في
`CLAUDE.md` عشان ما يتكرر.

**أمان:** البوت يتجاهل أي رسالة من محادثة غير `telegram_chat_id` — بدونها أي
شخص يلقى اسم البوت يقدر يشغّله ويصرف أرصدتنا.

### 10. المرحلة 3 خلصت — الفلترة الكاملة تشتغل ✅

**قائمة الأوامر في تيليجرام (طلبه):** الزر الأزرق يعرض الثمانية كلها، متأكد
منها بـ `getMyCommands`. **مبنية من نفس قائمة `COMMANDS` اللي تبني `/help`**
— مصدر واحد، فما يصير اختلاف بينهم لما نضيف أمر.

**تغيير مقصود على الخطة:** المواصفات كانت تقول "قائمة 50-100 سهم تُجهّز
يدوياً". بُنيت **آلياً** — لأن صاحب المشروع ما يقدر يعرف بنفسه أي أسهم قيمتها
السوقية بين 40 و 60 مليون، هذي بيانات ما تنكتب يدوياً. `/refresh` يمشي على
السوق كله (~ساعة ونص) في **خيط منفصل** عشان ما يجمّد باقي الأوامر.

**ترتيب الفحص مقصود لتوفير الأرصدة:** `quote` مجمّعة للكل أول (رصيد لكل
سهم)، وما نطلب `vwap` إلا لللي نجح في شرط الحجم.

**بق حقيقي كشفته الاختبارات قبل ما يوصل الجوال:** الكود كان يقرر شكل رد
Twelve Data من **عدد الرموز اللي طلبناها** (`len(batch) > 1`)، وهذا ينكسر لو
رجّع رمز ناقص. صار يتعرّف على الشكل **من محتوى الرد نفسه**.

**درس في تشخيص:** أول تشغيل حقيقي رجّع صفر تنبيهات حتى بنسبة 0.01، وشكّيت
إنه بق. الفحص طلع **رفض صحيح** — سعر AAPL كان 306.35 وVWAP‏ 306.50، يعني تحت
الخط. الفرق بين "ما اشتغل" و"اشتغل ورفض" ما ينكشف إلا بطباعة القيم الوسيطة.

**`test.py` صار 37 اختبار** (توقيت السوق + فلتر الحجم + خط أنابيب التنبيه
كامل ببيانات محضّرة). كلها تشتغل بدون إنترنت وبدون أرصدة.

**فخّ ثالث في SSH:** تشغيل شي في الخلفية عبر SSH **يعلّق الاتصال** لأن
العملية تمسك القناة. صار ثلاث مرات. الحل: `</dev/null` + أمر منفصل للتشغيل
وآخر للتحقق. مسجّل في `CLAUDE.md`.

### 11. المرحلة 4 خلصت — وقف الخسارة بـ ATR ✅

طلب "كمل" بعد المرحلة 3، فانتقلنا للمرحلة 4 مباشرة.

**الحساب:** `سعر الدخول − (ATR × 1.75)` — المضاعف قابل للتعديل من
`config.json`، والمواصفات تقول يبدأ بين 1.5 و 2. ATR يومي بفترة 14، من
Twelve Data (`atr` endpoint) — نفس النقطة اللي اختبرناها بالمفتاح الحقيقي في
جلسة سابقة.

**قرار مقصود لو فشل الطلب:** التنبيه **يوصل بدون ما يسقط بالكامل**، برسالة
تحذير بدل رقم وقف الخسارة. المنطق: الشروط الأربعة تحققت فعلاً، فالتنبيه
حقيقي — فقدان تفصيلة واحدة ما يستاهل حجب التنبيه كله عن صاحب المشروع.

**التكلفة:** رصيد Twelve Data إضافي واحد لكل تنبيه فقط — يشتغل بعد الشروط
الأربعة، وهذي نادرة أصلاً، فما يأثر على ميزانية الـ٨٠٠ رصيد اليومية.

**رابط TradingView أُضيف لكل تنبيه** — كان في مواصفات المشروع الأصلية («رابط
مباشر لشارت TradingView مع كل تنبيه») ولم يكن مبنياً، لاحظته أثناء صياغة
`format_alert()`.

**`test.py` صار 40 اختبار** — أضيف: فشل ATR ما يسقط التنبيه، وحساب وقف
الخسارة بالضبط (`entry=10.5 ATR=2.0 x1.5 → 7.5`، مؤكد رقمياً مو بالعين).

**البوت أعيد تشغيله على الجوال بكود المرحلة 4، وتأكدنا إن `/status` و
`/price` سليمين.** القائمة لسه فاضية — صاحب المشروع ما شغّل `/refresh` بعد.

**بقيت خمس مراحل من ست تشتغل. المرحلة 5 (بوت إدارة المخاطر) تحتاج حساب
تداول فعلي — مو شي نقدر نبنيه من عندنا.**

### 12. مشاركة صديقه + قائمة يدوية + أهداف ربح — ثلاث طلبات، ثلاث قرارات

طلب "تعديلات نجهزها قبل ما نرسلها للبوت" — قبل ما أبني أي شي، وضّحت ثلاث
نقاط كانت غامضة كفاية إنها تغيّر البناء بالكامل لو خمّنتها غلط:

1. **مشاركة الصديق: نفس البوت يخدم الاثنين** (اختار صراحة، مو بوت مستقل له
   كما كان مخطط بجلسة سابقة). حل مشكلة حقيقية: كيف نعرف chat_id صديقه بدون
   ما نوقف البوت يدوياً؟ **البوت نفسه يبلّغ صاحب المشروع** لما شخص غير
   مصرح له يراسله.
2. **القائمة اليدوية تتأكد من الرموز عبر Finnhub** (مو تُقبل مباشرة) — نفس
   شرط ١ و٢، بس بدون مسح السوق كله (١٠٬٠٠٠ رمز → رموزه بس).
3. **أهداف الربح: نسبة من سعر الدخول، مو من ATR** — رغم إن طلبه الأصلي
   قال "بناء على ATR". اختبرت الحساب رقمياً قبل ما أبني: ATR (كسور دولار)
   × نسبة صغيرة = رقم تافه ما له معنى كهدف ربح. النسب المستديرة (٥٪ ١٠٪
   ٢٠٪ ٢٥٪ ٥٠٪) هي المعنى القياسي في التداول، فبُنيت عليه مباشرة وذُكر
   الافتراض بوضوح — **قابل للتصحيح لو غلط**.

**اللي انبنى:**
- `allowed_chat_ids()` — قائمة `telegram_chat_ids` جديدة، توافق خلفي كامل
  مع `telegram_chat_id` القديم (اختُبر الاثنين).
- `broadcast()` — كل تنبيه وتقدم `/refresh` يوصل للكل. **ردود الأوامر
  ترجع لطالبها بس**، مو بث للكل.
- **تبليغ الغريب:** رسالة غير مصرح لها → البوت ما يرد على المرسل (أمان)،
  بس يرسل لصاحب المشروع اسم ورقم المرسل عشان يضيفه بضغطة.
- `/setlist AAPL,TSLA,...` — يستبدل القائمة بالكامل، يتأكد من كل رمز، يقرر
  تقرير كامل: كم دخل، كم رُفض ولِيش.
- `compute_take_profit_targets()` — تصاعدي دايماً بغض النظر عن ترتيب
  الإدخال (اختبرته بترتيب معكوس عمداً). ما يحتاج طلب شبكة إضافي.

**`test.py` صار ٤٦ اختبار** — أضيف: `allowed_chat_ids` (٣ حالات توافق)،
حساب أهداف الربح (رقمي مؤكد)، والتنبيه يحمل أهداف ربح صحيحة داخل خط أنابيب
الفحص الكامل.

**ملاحظة تصميم:** `/mute` وحالة الفحص مشتركة بين الاثنين (ملف `state.json`
واحد على الجهاز) — لم يُطلب فصلها ولا صلاحيات متفاوتة، فما بُنيت. لو احتاجها
لاحقاً تحتاج تصميم إضافي.

### 13. تراجع صريح عن `/setlist` وأهداف الربح — أمره: "زي ما هو إلا مشاركة البوت"

بعد نشر الميزات الثلاث (جلسة 12)، طلب صراحة: **لا تطبق هذه التعديلات إلا
لما أقلك، خلي كل شي زي ما هو ما عدا مشاركة البوت.** أعطى رقم هاتف صديقه
لإضافته.

**نُفّذ فوراً:** `/setlist` و`compute_take_profit_targets()` وكل ما يخصهما
**أُزيلا بالكامل** من `bot.py` و`test.py` و`config.example.json` —
`DEFAULTS`، الدالة، الأمر في `COMMANDS`، السطر في `handle_command`، والحقل
في التنبيه. مشاركة البوت (`allowed_chat_ids` و`broadcast` وتبليغ الغريب)
بقيت كما هي. `test.py` رجع لـ٤٣ اختبار (٤٦ ناقص ٣ خاصة بأهداف الربح).

**رقم الهاتف وحده ما يكفي.** بوتات تيليجرام ما تقدر تحوّل رقم هاتف إلى
chat_id — قيد من تيليجرام نفسه لحماية الخصوصية، ما له حل من جهة البوت.
الطريقة الوحيدة: الصديق يراسل `@Stock7oot_bot` بنفسه ولو مرة، وحينها آلية
"تبليغ الغريب" المبنية سابقاً تكشف اسمه ورقمه تلقائياً لصاحب المشروع.

### 14. حادثة حقيقية: إعادة التشغيل للتراجع أعلاه مسحت فحص `/refresh` كان وصل 1500/18424

نفس إعادة التشغيل اللي نفّذت التراجع (بند 13) **قتلت `/refresh` كان شغّال
منذ 08:12، وصل 1500 من 18,424 سهم، ولقى 25 سهم مطابق** — كلهم راحوا لأن
القائمة كانت تُحفظ بس في نهاية `build_watchlist()`، والمهمة تشتغل في خيط
داخل العملية، فإعادة تشغيل العملية تقتله بدون أي أثر. ولا طريقة لاسترجاع
الـ25 لأن أسماءهم ما كانت محفوظة بأي مكان — حتى رسائل التقدم في تيليجرام
كانت عدّاد أرقام بس، مو قائمة رموز.

**تفصيل ثانٍ اكتُشف بالمناسبة، مو له علاقة بالحادثة لكنه خطأ موثّق كان
موجود من جلسة سابقة:** المدة الموثّقة "ساعة ونص" كانت **تقدير نظري خاطئ**
مبني على افتراض ~10,000 سهم بمعدل 60 طلب/دقيقة. الرقم الحقيقي (مقاس من
رسائل التقدم الفعلية): **~18,400 سهم**، وبمعدل فعلي **~33-40 طلب/دقيقة**
فقط رغم إن الحد يسمح بـ60 — لأن الكود القديم كان يضيف ثانية كاملة **فوق**
وقت الرد نفسه، مو يحسبها من بداية الطلب. المدة الحقيقية القديمة: **~7.6
ساعة**، مو ساعة ونص.

**إصلاحان في `build_watchlist()`:**
1. **حفظ تدريجي** — `save_watchlist(found)` تُستدعى فور كل سهم جديد يدخل
   القائمة، مو مرة وحدة بالنهاية. حادثة زي هذي بعدها تخسر أقصى شي دقيقة
   وحدة من الشغل.
2. **تسريع صحيح** — الانتظار يُحسب من بداية الطلب السابق (يستهدف 55
   طلب/دقيقة، هامش أمان تحت حد 60)، مو ثانية إضافية فوق وقت كل رد. قرّب
   المدة من ~7.6 ساعة إلى **~5.6 ساعة** تقريباً.

**اختُبر بدون شبكة:** اختبار جديد في `test.py` يستبدل `finnhub_request` و
`us_common_stocks` بنسخ وهمية، ويتأكد إن `save_watchlist` تُستدعى بعد كل
سهم جديد (مو بس بالنهاية) وإن القائمة على القرص تطابق النتيجة. `test.py`
صار ٤٥ اختباراً. كل التوثيق ("ساعة ونص") صُحح في `README.md` و`CLAUDE.md`
مع الرقم الحقيقي وسبب الفرق، والحادثة نفسها مسجّلة في `CLAUDE.md` كتحذير:
لا تعيد تشغيل البوت أثناء `/refresh` بدون داعي — تحقق من `BUSY.get("refresh")`
أول.

### 15. توضيحان بعد الحادثة: مؤجل مو ملغي + نسخة احتياطية للقائمة

**١. `/setlist` وأهداف الربح: مؤجلان، مو ملغيان.** أكّد بنفسه: "التعديل اللي
طلبته منك اعتبره فورك، لا تنساه، بعدين بنطبقه." الفرق عن التراجع في بند 13
مهم — الكود القديم باقٍ في تاريخ git كنقطة بداية جاهزة، لكن **ما يُعاد بناؤه
تلقائياً**، ينتظر إشارته.

**٢. نسخة احتياطية للقائمة + `/restore`.** ردّة فعل مباشرة على حادثة بند 14
— طلب: "احفظ نسخة من آخر قائمة... يرجعلها إذا ما عجبته القائمة." كشف خطر
ثانٍ غير الانقطاع: `/refresh` يستبدل القائمة بالكامل من الصفر حتى في
التشغيل الطبيعي، فلو القائمة الجديدة أسوأ، ما في رجعة بدون هذي الإضافة.

**اللي انبنى:**
- `backup_current_watchlist()` — تُستدعى مرة وحدة قبل ما `/refresh` يبدأ
  الخيط (مو داخل `build_watchlist()`)، تنسخ القائمة الحالية لملف احتياطي
  وحيد (مو تاريخ كامل).
- `/restore` — يرجّع القائمة الفعلية للنسخة الاحتياطية. يتعامل بوضوح مع
  حالة "ما في نسخة أصلاً" (أول تشغيل). محمي بنفس قفل `BUSY.get("refresh")`.
- رسالة نجاح `/refresh` تذكّر بـ`/restore` صراحة.

**`test.py` صار ٤٨ اختبار** — ٣ حالات جديدة: بدون قائمة قديمة ما ينسخ شي؛
القائمة القديمة تبقى محفوظة حتى بعد استبدال الأساسية؛ `/restore` يرجّع
الأساسية فعلياً. كلها بدون شبكة.

## 2026-08-12 — حادثة استهلاك أرصدة Twelve Data + إصلاحها

### 16. حادثة حقيقية: الفحص التلقائي استمر يحاول بعد نفاد أرصدة Twelve Data اليومية، وصرف أرصدة سلبية بدون فايدة

فحص سجل البوت (`bot.log`) على الجوال كشف: أرصدة Twelve Data اليومية (800
طلب مجاني) نفدت بدري في يوم 2026-08-11، والبوت **استمر يحاول الفحص كل 15
دقيقة** (فترة `scan_interval_minutes`) لبقية دوام السوق، وكل محاولة فاشلة
كانت برضو تصرف رصيد واحد على الأقل قبل ما ترجع الخطأ — الأرصدة المستخدمة
طلعت فوق الحد فعلياً (801 ← 873 وطالعة). يعني آخر ساعتين تقريباً من وقت
السوق راحت هدر: صفر فحص فعلي، واستهلاك أرصدة زيادة بلا داعي.

**الإصلاح — ثلاث أجزاء في `bot.py`:**

1. **`is_credit_exhausted(message)`** — يتعرّف على رسالة Twelve Data
   المحددة ("run out of API credits") بدل ما يعامل كل خطأ نفس المعاملة.
2. **`state["twelvedata_blocked_until"]`** — أول ما تنفد الأرصدة، `start_scan()`
   يسجّل وقت التجدد (منتصف الليل **UTC**، مو منتصف الليل السعودي — فرق 3
   ساعات، والخلط بينهم كان يفتح الفحص بدري 3 ساعات قبل ما ترجع الأرصدة
   فعلياً عند Twelve Data). كل فحص تلقائي بعدها **يتجاهل بصمت** بدون ما
   يتصل بـ Twelve Data أصلاً، لين الوقت يعدّي. أمر `/scan` اليدوي أثناء
   الحظر يرد برسالة توضّح الوضع والوقت المتبقي، بدون ما يصرف رصيد إضافي
   بمحاولة فاشلة.
3. **تنبيه واحد بس** — أول نفاد أرصدة يبعث رسالة تيليجرام توضّح إن الفحص
   التلقائي بيتوقف لين الغد، بدل رسالة فشل جديدة كل 15 دقيقة.
4. **`/status`** يعرض حالة الحظر والوقت المتبقي لو كان فيه واحد شغّال —
   تنفيذ لملاحظة كانت مسجّلة من قبل ("`/status` لازم يزيد كم رصيد باقي").

**اختُبر (7 حالات جديدة، بدون شبكة):** `is_credit_exhausted` على 3 رسائل
(الرسالة الحقيقية، خطأ غير متعلق، بدون رسالة)؛ وسيناريو كامل بمحاكاة
`run_in_background` بشكل متزامن يتأكد إن أول نفاد أرصدة يسجّل وقت التجدد
وينبّه مرة وحدة، وإن الفحص التلقائي بعدها يتجاهل بصمت **بدون ما يعيد
الاتصال بـ Twelve Data**، وإن أمر `/scan` اليدوي أثناء الحظر يرد برسالة
واضحة بدون اتصال جديد. `test.py` صار **56 اختبار**.

### 17. تصحيح: `/refresh` ما له علاقة بأرصدة Twelve Data إطلاقاً

بعد نشر بند 16، مراجعة إضافية كشفت خطأين في نفس التوثيق:

**١. `/refresh` (`build_watchlist()`) يستخدم Finnhub بس** — القيمة السوقية
والفلوت من `profile2`، ولا يلمس Twelve Data أبداً. حظر `twelvedata_blocked_until`
**ما يوقف `/refresh` ولا يأخّره** — مسار منفصل تماماً. الخطة الأصلية في
نهاية بند 16 ("انتظر تجدد الأرصدة قبل `/refresh`") كانت غلط، وتصحّحت هنا
وفي `report.html`.

**٢. رصيد كان يُهدر بصمت في مرحلة `vwap`.** إصلاح بند 16 كان يكتشف نفاد
الأرصدة في مرحلة `quote` بس. لو نفدت أثناء مرحلة `vwap` تحديداً (بعد ما
`quote` نجحت)، الكود القديم كان يتعامل معها كأي رمز بدون بيانات vwap
(`continue`)، فـ `scan_watchlist()` يرجع بدون خطأ، والحظر ما ينضبط —
الفحص التالي يعيد نفس المحاولة الفاشلة. `scan_watchlist()` الحين يتحقق من
`is_credit_exhausted()` في المرحلتين، مو `quote` بس.

**اختُبر:** سيناريو جديد يخلي `quote` ينجح و`vwap` يرجع رسالة النفاد —
يتأكد إن `scan_watchlist()` يوقف ويرجّع الخطأ، مو يتجاهله. `test.py` صار
**57 اختبار**.

**٣. توضيح وثّقته `CLAUDE.md`:** افتراض "منتصف الليل UTC" لتجدد أرصدة
Twelve Data **غير مؤكد ميدانياً بعد** — لا صفحة أسعار ولا اختبار حقيقي
يثبته، بس الفشل متسامح بالاتجاهين. يُتأكد منه غداً من وقت أول فحص ينجح
بدون خطأ.

### الجلسة الجاية

**نشر هذا التصحيح على الجوال وإعادة تشغيل البوت.** بعدها: **تشغيل
`/refresh` الحين — ما ينتظر أرصدة Twelve Data**، لأنه ما يلمسها أصلاً.
الوقت الحالي (السوق مقفل، منتصف الليل تقريباً) مثالي لبدء فحص الـ٥-٦ ساعات.
الفحص السابق من 2026-08-11 ضاع بسبب إعادة تشغيل قديمة (بند 14)، يبدأ من
الصفر، لكن هذي المرة أي انقطاع ما يخسر التقدم، والنسخة الاحتياطية تحمي
القائمة الحالية اللي فيها 50 سهم بس.

**قرار مطلوب من صاحب المشروع قبل أو بعد `/refresh`:** القائمة الحالية (50
سهم) مع فترة فحص 15 دقيقة تستهلك أرصدة Twelve Data اليومية خلال ~4 ساعات
بس من أصل ~6.5 ساعة دوام السوق (50 رصيد/فحصة × 26 فحصة مطلوبة/يوم = 1300
رصيد، والمتاح 800). يعني حادثة بند 16 **بتتكرر كل يوم سوق** بنفس الإعدادات
الحالية — الإصلاح أوقف الهدر، لكن ما حل قصر الميزانية نفسه. خياران من نفس
جدول `report.html` ("كم سهم نقدر نغطي"): رفع `scan_interval_minutes` لـ 30
(القائمة الحالية)، أو تحديد حجم القائمة عند 20-30 سهم بعد `/refresh`
(المقايضة: قائمة أطول = تنبيه أبطأ لتنبيه مبني على قفزة حجم). هذا اختياره،
مو قرار تقني.

صاحب المشروع يطلب من صديقه يراسل البوت لإضافته. المرحلة 5 تنتظر قراره —
تحتاج حساب تداول فعلي.

### 19. حادثة حقيقية: `/list` كان يقصّ القائمة عند 40 سهم بصمت

جرّب صاحب المشروع `/list` على القائمة القديمة (50 سهم) ولاحظ إنها توقفت
عند 40 سهم وكتبت "... و 10 غيرهم" بدل ما تعرضهم فعلياً — **إخفاء بيانات
بصمت**، مو خطأ واضح. السبب: `cmd_list()` كانت تقص عند 40 عنصر يدوياً،
على الأغلب احتياط قديم من حد طول رسالة تيليجرام (4096 حرف) بدون حل جذري.

**الإصلاح — عام، مو خاص بـ`/list` بس:**
- `chunk_message(text, limit=4096)` — تقسّم أي نص طويل لعدة أجزاء عند
  حدود الأسطر (ما تقطع سطر من نصه)، تحت حد تيليجرام الفعلي.
- `send_message_chunked()` — ترسل كل الأجزاء بالترتيب. حلّت محل
  `send_message()` في مسارين: رد الأوامر المباشر (`handle_command`) و
  `broadcast()` — يعني أي أمر أو تنبيه مستقبلي يطول، ينقسم تلقائياً، ما
  يُفقد ولا يُقصّ.
- `cmd_list()` رجعت تعرض القائمة **كاملة** بدون حد 40.

**اختُبر (3 حالات):** `chunk_message` يقسّم نص طويل بدون فقدان أي سطر
(التحقق: دمج الأجزاء يرجّع النص الأصلي بالضبط) وبدون تجاوز الحد؛ نص قصير
يرجع بجزء واحد بدون تغيير؛ `cmd_list()` بقائمة 45 سهم تُظهر الـ45 كاملة
وما فيها كلمة "غيرهم". `test.py` صار **60 اختبار**.

### 18. قراره: تحديد حجم القائمة عند 30 سهم، مو رفع فترة الفحص

بين الخيارين في بند 17، اختار صراحة: **يحدد القائمة بنفسه، وحد أقصى 30
سهم "عشان ما ينفذ"** — يعني يفضّل تنبيه أسرع (فحص كل 15 دقيقة كما هو) على
تغطية أوسع. `max_watchlist_size` في `config.json` على الجوال تغيّر من
الافتراضي (50) إلى **30**. `config.example.json` تحدّث بنفس القيمة عشان أي
جهاز جديد (جوال صديقه) يرث نفس القرار.

**تنبيه تقني مهم:** `load_config()` تُقرأ **مرة وحدة عند بدء تشغيل البوت**
(`main()`)، مو مع كل أمر. تعديل `config.json` وحده **ما يكفي** — لازم
إعادة تشغيل البوت عشان القيمة الجديدة تُطبّق. **تم فعلاً** — البوت أُعيد
تشغيله بعد التعديل (٠٧:٢٤).

٣٠ سهم × ٢٦ فحصة/يوم (كل ١٥ دقيقة) = ٧٨٠ رصيد — تحت حد ٨٠٠ بهامش ضيق (٢٠
رصيد). لو `/refresh` طلّع أقل من 30 سهم مطابق فعلياً، الميزانية بتكون أوسع
تلقائياً.
